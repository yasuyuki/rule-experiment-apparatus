# CONSTITUTION — ルール実験装置

この文書は `rule-experiment-system` システム（`rule-experiment-apparatus` +
`foundation-control`）が**何のために存在するか**の正本である。

用語は [`TERMS.md`](TERMS.md) が定義する。本文は実験の語彙（base・baseline・
variant・アーム・固定・照合）で書く。

装置を操作する主体を `controller`、注入済み変種の下で逐語の `workload.md` を
実行する被験主体を `subject`、実験を回せる状態を作る主体を `provisioner` と呼ぶ。

実験の単位は **実験 / サイクル / アーム / 実行** の階層で数える。各語の定義と
階層図は `TERMS.md`。

アームは同一の workload repository commit（base）から起こし、各アームへ rule-source
tree を注入する。baseline は検証済みの stable rule-source tree であり、base とは別物である。

計測サイクルは対照アーム1件と処置アーム1件、推定サイクルはアーム1件である。
隔離は arm と subject ごとの config root が担う。

固定（promote）の対象にできるアームは、一度に1つである。

存在理由: この装置は「リリース管理ツール」として読まれ続けてきた。目的が
どこにも記録されていないため、実験と無関係な機能・ワークフローが足される余地が
残っている。以下を明文化してその余地を塞ぐ。

**この憲法が拘束するのは、装置を開発・拡張する側**（装置リポジトリで作業する
セッション、`cycle.py` の変更者、フェーズを実行するエージェント）**であって、
subject 側ではない。** アームの中の実行はこの憲法を読まない。読ませると
全実験に共通の交絡因子が入る（不変条件 8）。

境界を面で描くとこうなる（説明図であって、新しい用語ではない）。

```text
装置側（この憲法が拘束する）        実験側（拘束しない・読ませない）
  controller / provisioner            subject
  baseline とアームの構築              注入済み変種
  計測仕様・宣言・記録・状態遷移        逐語の workload.md の実行
```

---

## 1. 何であるか

**ルール変更がエージェントの行動を変えるかを計測する装置。**

`materialize` / `handoff` / `judge` / `promote` / `rollback` は目的ではなく、
「同一 base から独立したアームを起こし、結果を記録し、検証済み source を
baseline に反映する」ための実装である。

装置が回す操作は次の4つだけ。これ以外の操作を装置の機能として増やさない。

| 操作 | 何をするか |
|---|---|
| **固定** | 効果が確認できた変種をベースラインに入れ、以後は自動的に載るようにする |
| **再計測** | 同一変種・同一 workload で再実行し、再現性を見る |
| **改良再実行** | 変種だけを変えて再実行し、差分を見る |
| **推定** | 再実行が非現実的な workload の1実行から、凍結入力に推定器を当てて変種の効果を見積もる |

各操作の手順は `docs/RULE-EXPERIMENT.md` §5、推定の設計は同 §7。

再計測は「同一変種・同一 workload で再実行できる」ことを前提にする。workload が
巨大で再実行が非現実的なとき、この前提が崩れる。推定はその場合の縮退操作であり、
**計測の代替ではない**。推定の結果は本計測（対照アームつきサイクル）の対象選定
（triage）にのみ使い、固定の根拠にしない（不変条件 10）。逐語の `workload.md` が
そもそも無い一回性の作業と、照合が落ちたサイクルの救済は、推定の対象ではない。

対象ルールは GUI 確認、サブエージェント協働、ファイル参照フィルターなど
**内容が一貫しない**。内容が不定である以上、計測基準を事前に
確定できない。これが不変条件 2 の根拠である。

### 前提条件を確立する操作

上の4つは**実験を回す操作**である。これとは別に、**実験を回せる状態を作る操作**が要る。
両者を混ぜない。

- subject の追加・変更（実証済み配置経路の確立を含む）
- 分離状態の確立と復旧（不変条件 8）
- 装置自身の環境整備
- 推定器の較正（計測済みサイクルを正解データとして推定器の信頼性を確認する。
  不変条件 10）

この区分に §5 の適合判定は適用しない。代わりに次の3つで縛る。

1. **実験サイクル中に実行しない。** 比較キー（不変条件 1）を動かすため
2. **結果を宣言へ書く。** 実行しただけで終わらせない
3. **未実証のまま `proven` にしない。** subject の自己申告ではなく、marker が実際に
   出ることを確認してから（不変条件 4）

この区分を、4操作を増やすための抜け道にしない。**ここに入るのは「装置がまだ実験を
回せない」状態を解消する作業だけ**であり、実験の回し方を良くする作業は上の4操作である。

## 2. 用語

**正規用語は [`TERMS.md`](TERMS.md) を正本とする。** 実験の単位（実験 / サイクル /
アーム / 実行）、主体（controller / subject / provisioner）、比較・計測・推定に
関する語および用語の規律はすべてそこにある。

この憲法・下位文書・新設する文書は、`TERMS.md` にある概念を別の語で言い換えない。
`TERMS.md` は用語の意味だけを定め、**原則を足したり緩めたりしない。**

## 3. 何ではないか（非目標）

次はいずれも**この装置の目的ではない**。該当する機能追加は、他にどれだけ
正当な理由があってもここへ足さない。

- **汎用リリース管理・デプロイ基盤ではない。** `materialize` は実験アームを
  作る手段であって、成果物を配る仕組みではない
- **ルール配布機構ではない。** 変種を複数環境へ行き渡らせることは目的ではない。
  注入は計測のための一時的な操作である
- **CI ではない。** 自動実行・定期実行・パイプライン化は目的ではない。1サイクルは
  人間が回す計測であって、通す／落とすを判定する門ではない
- **特定の計測手段を焼き込む場所ではない。** stamp・ログ形式・判定スクリプトなどを
  機構側へ焼き込まない（不変条件 2）
- **汎用 LLM 評価基盤ではない。** 推定器は差し替え可能な道具であって、推定器の
  プラットフォーム・プロンプト資産・評価スイートを作り込む場所ではない。装置が
  持つのは凍結入力・推定記録の形・推定器 identity の記録だけである（不変条件 10）
- **subject product の開発基盤ではない。**
  アプリは被験体であって目的ではない。アプリの機能を良くするための機能は
  この装置に足さない
- **開発環境を規定する場ではない。** アプリや装置の開発をどの環境（Windows / WSL /
  その他）で行うかは作業者が選ぶ。**環境を自由に選べること自体が前提であり、装置は
  その選択を制約せず、特定の環境を前提にもしない。** 環境固有の値は
  environment descriptor が持つ。core executor は WSL または local POSIX とする

## 4. 不変条件

1つでも崩れたら、そのサイクルの結果は比較対象として使えない
（不変条件 10 が崩れた場合は、その推定が triage に使えない）。

### (1) 1サイクル1変数 — 比較条件は宣言し、宣言どおりであることを照合する

比較キーは次で構成する。

- base（全アームが共有する workload repository commit）
- 変種
- `workload.md`
- 計測定義（計測仕様の版）
- subject（1アームを複数の subject が触ってよい。各種別と、隔離先・identity の識別。不変条件 9）
- アームに載る常時適用ルールの集合
- 実験メタ情報の可読性（不変条件 8(3)）

1サイクルで変えてよいのはこのうち1要素だけ。2つ以上動いたら、再計測・改良再実行では
なく**別実験**として扱う。

**交絡は除去対象ではなく宣言対象である。** 装置の責務は交絡を無くすことではなく、
交絡を宣言させ、サイクル間で不変であることを機械照合することにある。宣言に無い
ものがアームに載っていたら、そのサイクルは比較に使えない。「載っていることを誰も
知らない」状態が、比較可能性を壊す。

**例外は1つ。測ろうとしている内容と同種の交絡は、宣言では足りない。** 対照アームが
既に処置済みになり、その変種の効果を測れなくなる。この場合は除去する。除去は前提条件を
確立する操作であって実験ではない（§1）。

計測器と被計測物を同時に変えない。証拠を取るために対象 repo の変更が要るなら、
その変更自体を別サイクルの実験対象へ分離する。判定手段がアームごとに違ってはならない。

各要素を具体的にどう宣言し、宣言と実状態をどう照合するかは
`docs/RULE-EXPERIMENT.md` §4。

### (2) 計測基準は内容ごとに導出する。計測手段を機構へ焼き込まない

`obeyed`（行動が変わったか）と `attributable`（ルールが無くても同じ行動を
しなかったか）の判定は、**ルール本文を見て実験ごとに導出する**。判定方法を
core・スキーマ・テンプレートへ焼き込まない。

唯一の例外は `loaded`（ルールが文脈に入ったか）で、これは全変種共通の marker
`[<experiment>:<variant>]` を1回出す指示として**装置が共通に提供する**。
実験ごとに設計しない。この例外を他の軸へ広げない。

### (3) controller と subject の責務を混ぜない

自分で書いた規則に従うことは、初見のセッションが規則に従うかという計測対象と
別物である。変種 / workload / 計測 / 記録 / 状態遷移は controller が扱う。
subject は注入済み変種の下で逐語の `workload.md` だけを実行する。
実験運用上 subject へ渡す文書はこの2つに限り、変種正本、計測仕様、
この憲法、記録を作成・変更させず、読ませない。

`provisioner` は前提条件を確立する操作（§1）だけを行う。**変種正本・`workload.md`・
計測仕様・記録・baseline を変更しない。** subject 環境を触るが、実験サイクル中には
実行しない。通常の開発作業と許可・禁止の基準が違うため、controller と subject の
どちらにも寄せない。

### (4) 未実証の配置経路に変種を置かない

**実証済みの配置経路は subject ごとに異なる。** 装置は subject ごとに実証済み
配置経路の集合を持ち、その集合以外へ変種を置かない。未実証の配置経路へ置くと、
null 結果が「効かなかった」のか「載らなかった」のか区別できず、そのサイクルが
無駄になる。**この憲法は特定の配置経路を名指ししない。**

**新しい subject は `proven: false` から始まる。** marker だけを載せた変種で、実際に
marker が出ることを確認してから `proven` にする。この確認は前提条件を確立する操作で
あって実験ではない（§1）。subject 自身の申告（`status` 等）を根拠にしない。

**有効な配置経路が複数ある subject では、使う1つを決め、残りを空に保つ。** どこに何が
あるか分からない状態は、載っていない状態と区別できない。

実証の手順、subject 記述子による配置経路の保持、marker が出なかったときの扱いは
`docs/RULE-EXPERIMENT.md` §8。

### (5) 変種の正本はアーム worktree の外に置く

`materialize` は宣言された base からアームを毎回作る。アームだけに手打ちした変種は
次のサイクルに残らない。だから変種の正本はアーム worktree の外の版管理下に置き、
**毎サイクルそこから注入する。手打ちしない。**
正本の同一性は内容から決まる識別子で表し、注入のたびに照合する。

正本の現在の置き場、同一性の表し方、照合の実装は `docs/RULE-EXPERIMENT.md` §5.1。

### (6) base は全アームで共有し、baseline に直接書き込ませない

同一サイクル内の全アームは同じ base commit から起こす。base が異なれば、workload
repository の差分と variant の差分を分離できない。baseline は検証済み stable
rule-source tree であり、公開機構はこれへの直接書き込みを拒否しなければならない。
内部実装にも、任意の baseline 変更を公開操作へ迂回させる抜け道を作らない。

baseline を変える唯一の入口は promote（＝固定）と rollback（固定の取り消し）である。

### (7) workload は不変に保ち、版管理する

実行へ渡す作業指示は `workload.md` に置き、口頭・その場の言い換えで渡さない。
同じ文字列を渡していないなら、2回の実行は比較できない。

### (8) 憲法および実験メタ情報の可読性を宣言し、不変に保つ

1. 憲法、変種正本、計測仕様、記録は、subject の**常時適用経路**に載らない。
   常時適用経路は subject ごとに定義され、**装置がその全内容を構成する**
2. subject へ渡す `workload.md` は、それらを読む作業を含まない
3. 装置自身を**被験体**にするサイクルでは (2) を守れない。この場合、**憲法可読を
   交絡として宣言し、比較キーに含める。同じ宣言を持つサイクル同士でのみ比較する**

常時適用のルールを無宣言でアームへ置くと、**全実験に共通の交絡因子**が入り、以後どの
実験も「変種の効果」とそれ以外を分離できなくなる。禁じているのはファイルの存在では
なく、**宣言されていない可読性**である（不変条件 1）。

### (9) subject identity を記録し、handoff で target を照合する

比較可能なアーム間では、そのアームを触る各 subject の**隔離先**（その subject の
config root）と、その中身の識別を不変に保つ。1アームを複数の subject が触る場合も、
識別は subject ごとである。識別は**内容のハッシュ**で表し、実行しただけで変わる値
（mtime など）を含めない。**実行が identity を壊す定義は、比較条件ではなく計測の
副作用を測っている。**

subject は1種類ではなく、増える。隔離手段・実証済み配置経路・identity に含める項目は
subject ごとに異なるが、それは装置が持つ**データ**であって、subject ごとに機構を
足す理由にはならない。

**ツールの版はピン留めしない。** 版のピン留めは実運用のリスクであり、版の不一致は
交絡リスクとして受容する。ただしアーム開始時に各 subject の版を**記録する**
（不変条件 1 の宣言）。記録が無いと、再計測が一致しなかったときに版変化を候補から
外せない。既知の不具合の回避手順が特定の版に依存する場合だけ、その版を明示して
照合する。

handoff はアームの target と subject identity が宣言どおりであることを照合してから
人へ渡す境界である。これは計測結果でも特定の計測方式でもなく、比較条件の照合である。

identity に何を含めるか、handoff が具体的に何を照合するかは
`docs/RULE-EXPERIMENT.md` §8。

### (10) 推定を計測に混ぜない

再計測は同一条件で再実行できることを前提にする。workload が巨大で再実行が
非現実的なとき、装置は1実行の痕跡から効果を**推定**できるが、推定は計測と
別クラスの主張である。次を1つでも守らない推定は、triage にも使えない。

- **推定記録は記録（計測ログ）と別置する。** 推定を固定（promote）の根拠にしない。
  用途は本計測（対照アームつきサイクル）の対象選定（triage）に限る
- **入力は凍結する。** 推定に使う痕跡一式を hash で識別し、推定器はその凍結入力
  だけを読む。workload の再現性が失われても、同じ凍結入力へ別の推定器を後から
  当て直せることで、**再現性の担保対象を実行から推定へ移す**
- **推定器の identity を記録する。** どの推定器の主張かを後から復元できる識別子を
  推定記録に残す。identity の無い推定は、その推定が誰の主張かを復元できない
  （identity の構成は `docs/RULE-EXPERIMENT.md` §7.3）
- **未較正の推定器を使わない。** 推定器は較正（§1 の前提条件を確立する操作）で
  計測済みサイクルの結果と突き合わせてから使う。未較正の推定器の null 推定は
  「効果が無かった」のか「推定器が見つけられなかった」のか区別できない
  （不変条件 4 と同型）

推定器の**中身**（LLM か、統計か、ヒューリスティックか）は装置へ焼き込まない
（不変条件 2 と同型）。装置が固定するのは凍結入力・推定記録の形・推定器 identity の
記録の3点だけである。凍結入力の束・推定器 interface・較正の手順は
`docs/RULE-EXPERIMENT.md` §7。

## 5. 機能追加の適合判定

改善・削減・機能追加の判定は [`docs/IMPROVEMENT-POLICY.md`](docs/IMPROVEMENT-POLICY.md)
を唯一の正本とする。具体的な未着手欠陥は運用側の `ISSUES.md` に置き、この repository には置かない。

---

関連文書と authority の方向。**下位文書は上位の規範を具体化するだけで、下位の
記述によって不変条件や非目標の意味は変わらない。**

**これは authority の階層であって網羅索引ではない。** 版管理下の文書の索引は
`README.md` の1表だけであり、その網羅性は `apparatus/docs_check.py` が照合する。

```text
CONSTITUTION.md                この装置の identity。目的・境界・不変条件・適合判定
    ↓
TERMS.md                       正規用語の正本
docs/IMPROVEMENT-POLICY.md     core 境界と改善判定の正本
docs/RULE-EXPERIMENT.md        不変条件を成立させるプロトコルの正本
docs/EXECUTION-UNIT.md         実行の所属基準の正本（executionUnitHash で pin）
docs/ARM-SCOPE.md              アームの可視範囲と workload 適格性の正本
    ↓
docs/USER-GUIDE.md             利用者・エージェントが実際に行う操作手順
docs/REVIEW-CRITERIA.md        記録（計測ログ）の記入基準
docs/CYCLE-RECORD-CRITERIA.md  サイクル記録の判定基準
implementation                 `apparatus/`
```

この憲法を controller へ効かせる手段は、このリポジトリの親ワークスペースにある各ツールの
常時適用ルールである。Claude Code / Cursor / Codex で置き場が異なるため1箇所ではない。
索引は同じ階層の `AGENTS.md`。
